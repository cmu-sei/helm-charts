apiVersion: v1
kind: Service
metadata:
  name: {{ include "foundry-buckets-api.fullname" . }}
  labels:
    {{- include "foundry-buckets-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "foundry-buckets-api.selectorLabels" . | nindent 4 }}
