apiVersion: v1
kind: Secret
metadata:
  name: {{ include "foundry-buckets-api.fullname" . }}
  labels:
    {{- include "foundry-buckets-api.labels" . | nindent 4 }}
type: Opaque
stringData:
{{- range $key, $val := .Values.env }}
  {{ $key }}: {{ $val | quote }}
{{- end }}
