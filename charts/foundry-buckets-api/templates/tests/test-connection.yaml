apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foundry-buckets-api.fullname" . }}-test-connection"
  labels:
    {{- include "foundry-buckets-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "foundry-buckets-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
