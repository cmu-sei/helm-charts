apiVersion: v1
kind: Service
metadata:
  name: {{ include "foundry-comms-api.fullname" . }}
  labels:
    {{- include "foundry-comms-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "foundry-comms-api.selectorLabels" . | nindent 4 }}
