apiVersion: v1
kind: Service
metadata:
  name: {{ include "caster-psql.fullname" . }}
  labels:
    {{- include "caster-psql.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "caster-psql.selectorLabels" . | nindent 4 }}
