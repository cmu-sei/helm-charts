apiVersion: v1
kind: Secret
metadata:
  name: {{ include "caster-psql.fullname" . }}
  labels:
    {{- include "caster-psql.labels" . | nindent 4 }}
type: Opaque
stringData:
{{- range $key, $val := .Values.env }}
  {{ $key }}: {{ $val | quote }}
{{- end }}