apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "caster-psql.fullname" . }}-test-connection"
  labels:
    {{- include "caster-psql.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "caster-psql.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
