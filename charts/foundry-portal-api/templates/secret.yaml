apiVersion: v1
kind: Secret
metadata:
  name: {{ include "foundry-portal-api.fullname" . }}
  labels:
    {{- include "foundry-portal-api.labels" . | nindent 4 }}
type: Opaque
stringData:
{{- range $key, $val := .Values.env }}
  {{ $key }}: {{ $val | quote }}
{{- end }}
