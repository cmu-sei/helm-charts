apiVersion: v1
kind: Service
metadata:
  name: {{ include "foundry-portal-api.fullname" . }}
  labels:
    {{- include "foundry-portal-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "foundry-portal-api.selectorLabels" . | nindent 4 }}
