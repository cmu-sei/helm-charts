# Keycloak realm import is configured via the keycloak.extraEnvVars,
# keycloak.extraVolumes, and keycloak.extraVolumeMounts in values.yaml
# By default, realm import is disabled (commented out)
# Uncomment those sections to enable automatic realm import from a ConfigMap
