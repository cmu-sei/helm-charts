apiVersion: v1
kind: Service
metadata:
  name: {{ include "terraform-crucible-provider.fullname" . }}
  labels:
    {{- include "terraform-crucible-provider.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "terraform-crucible-provider.selectorLabels" . | nindent 4 }}
