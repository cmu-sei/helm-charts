apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "terraform-crucible-provider.fullname" . }}-test-connection"
  labels:
    {{- include "terraform-crucible-provider.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "terraform-crucible-provider.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
