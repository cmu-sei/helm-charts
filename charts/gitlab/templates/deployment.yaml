apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "gitlab.fullname" . }}
  labels:
    {{- include "gitlab.labels" . | nindent 4 }}
spec:
{{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
{{- end }}
  selector:
    matchLabels:
      {{- include "gitlab.selectorLabels" . | nindent 6 }}
  template:
    metadata:
    {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
    {{- end }}
      labels:
        {{- include "gitlab.selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "gitlab.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: {{ .Chart.Name }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          command: {{- toYaml .Values.command | nindent 10 }}
          envFrom:
            - secretRef:
                name: {{ include "gitlab.fullname" . }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 80
              protocol: TCP
          # livenessProbe:
          #   httpGet:
          #     path: /
          #     port: http
          # readinessProbe:
          #   httpGet:
          #     path: /
          #     port: http
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          volumeMounts:
            {{ if .Values.certificateMap }}
            - mountPath: /usr/local/share/ca-certificates
              name: certificates
            {{- end }}
            - mountPath: /var/opt/gitlab
              name: {{ include "gitlab.name" . }}-vol
              subPath: {{ include "gitlab.fullname" . }}/data
            - mountPath: /var/log/gitlab
              name: {{ include "gitlab.name" . }}-vol
              subPath: {{ include "gitlab.fullname" . }}/logs
            - mountPath: /etc/gitlab
              name: {{ include "gitlab.name" . }}-vol
              subPath: {{ include "gitlab.fullname" . }}/config
            - mountPath: /conf
              name: {{ include "gitlab.fullname" . }}-conf
            - name: gitlab-password
              mountPath: "/secrets"
      volumes:
        {{ if .Values.certificateMap }}
        - name: certificates
          configMap:
            name: {{ .Values.certificateMap }}
        {{- end }}
        {{- if .Values.storage.existing }}
        - name: {{ include "gitlab.name" . }}-vol
          persistentVolumeClaim:
            claimName: {{ .Values.storage.existing }}
        {{- else if .Values.storage.size }}
        - name: {{ include "gitlab.name" . }}-vol
          persistentVolumeClaim:
            claimName: {{ include "gitlab.fullname" . }}
        {{- else }}
        - name: {{ include "gitlab.name" . }}-vol
          emptyDir: {}
        {{- end }}
        - name: {{ include "gitlab.fullname" . }}-conf
          configMap:
            name: {{ include "gitlab.fullname" . }}-conf
        - name: gitlab-password
          secret:
            secretName: gitlab-root
            items:
            - key: initial_root_password
              path: initial_root_password
              mode: 0777
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
