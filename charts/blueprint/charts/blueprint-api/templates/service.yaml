apiVersion: v1
kind: Service
metadata:
  name: {{ include "blueprint-api.fullname" . }}
  labels:
    {{- include "blueprint-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "blueprint-api.selectorLabels" . | nindent 4 }}
