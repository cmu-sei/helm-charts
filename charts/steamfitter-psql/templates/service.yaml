apiVersion: v1
kind: Service
metadata:
  name: {{ include "steamfitter-psql.fullname" . }}
  labels:
    {{- include "steamfitter-psql.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "steamfitter-psql.selectorLabels" . | nindent 4 }}
