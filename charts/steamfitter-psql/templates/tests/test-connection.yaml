apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "steamfitter-psql.fullname" . }}-test-connection"
  labels:
    {{- include "steamfitter-psql.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "steamfitter-psql.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
