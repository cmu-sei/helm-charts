apiVersion: v1
kind: Secret
metadata:
  name: {{ include "steamfitter-psql.fullname" . }}
  labels:
    {{- include "steamfitter-psql.labels" . | nindent 4 }}
type: Opaque
stringData:
{{- range $key, $val := .Values.env }}
  {{ $key }}: {{ $val | quote }}
{{- end }}