apiVersion: v1
kind: Service
metadata:
  name: {{ include "mks.fullname" . }}
  labels:
    {{- include "mks.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mks.selectorLabels" . | nindent 4 }}
