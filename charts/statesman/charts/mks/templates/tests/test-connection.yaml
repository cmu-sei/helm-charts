apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mks.fullname" . }}-test-connection"
  labels:
    {{- include "mks.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mks.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
