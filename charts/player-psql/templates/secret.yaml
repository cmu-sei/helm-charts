apiVersion: v1
kind: Secret
metadata:
  name: {{ include "player-psql.fullname" . }}
  labels:
    {{- include "player-psql.labels" . | nindent 4 }}
type: Opaque
stringData:
{{- range $key, $val := .Values.env }}
  {{ $key }}: {{ $val | quote }}
{{- end }}