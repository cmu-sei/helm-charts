apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foundry-metrics-api.fullname" . }}-test-connection"
  labels:
    {{- include "foundry-metrics-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "foundry-metrics-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
