apiVersion: v1
kind: Service
metadata:
  name: {{ include "foundry-metrics-api.fullname" . }}
  labels:
    {{- include "foundry-metrics-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "foundry-metrics-api.selectorLabels" . | nindent 4 }}
