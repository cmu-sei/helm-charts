apiVersion: v1
kind: Secret
metadata:
  name: {{ include "foundry-metrics-api.fullname" . }}
  labels:
    {{- include "foundry-metrics-api.labels" . | nindent 4 }}
type: Opaque
stringData:
{{- range $key, $val := .Values.env }}
  {{ $key }}: {{ $val | quote }}
{{- end }}
