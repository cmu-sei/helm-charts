apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foundry-portal-ui.fullname" . }}-test-connection"
  labels:
    {{- include "foundry-portal-ui.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "foundry-portal-ui.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
