apiVersion: v1
kind: Service
metadata:
  name: {{ include "foundry-portal-ui.fullname" . }}
  labels:
    {{- include "foundry-portal-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "foundry-portal-ui.selectorLabels" . | nindent 4 }}
