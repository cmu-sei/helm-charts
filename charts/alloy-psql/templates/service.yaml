apiVersion: v1
kind: Service
metadata:
  name: {{ include "alloy-psql.fullname" . }}
  labels:
    {{- include "alloy-psql.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "alloy-psql.selectorLabels" . | nindent 4 }}
