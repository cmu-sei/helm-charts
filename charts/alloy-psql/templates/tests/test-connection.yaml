apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "alloy-psql.fullname" . }}-test-connection"
  labels:
    {{- include "alloy-psql.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "alloy-psql.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
