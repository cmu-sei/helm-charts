apiVersion: v1
kind: Service
metadata:
  name: {{ include "foundry-orders.fullname" . }}
  labels:
    {{- include "foundry-orders.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "foundry-orders.selectorLabels" . | nindent 4 }}
