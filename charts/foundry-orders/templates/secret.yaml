apiVersion: v1
kind: Secret
metadata:
  name: {{ include "foundry-orders.fullname" . }}
  labels:
    {{- include "foundry-orders.labels" . | nindent 4 }}
type: Opaque
stringData:
{{- range $key, $val := .Values.env }}
  {{ $key }}: {{ $val | quote }}
{{- end }}
