apiVersion: v1
kind: Service
metadata:
  name: {{ include "foundry-buckets-ui.fullname" . }}
  labels:
    {{- include "foundry-buckets-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "foundry-buckets-ui.selectorLabels" . | nindent 4 }}
